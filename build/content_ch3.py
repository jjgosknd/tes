"""Глава 3: Реализация и тестирование программной системы."""
from helpers import p, h1, h2, table, table_caption, fig_caption, listing, listing_caption
from diagrams import (diagram_screen_login, diagram_screen_catalog,
                      diagram_screen_course, diagram_screen_lesson,
                      diagram_screen_profile)


def chapter3():
    out = []
    out.append(h1("Глава 3. Реализация и тестирование программной системы"))

    # =================== 3.1 ===================
    out.append(h2("3.1 Описание структуры проекта"))

    out.append(p("Программная система реализована в виде клиент-серверного "
                 "решения, состоящего из мобильного клиента под Android и "
                 "REST-сервиса серверной части. Мобильный клиент построен "
                 "как нативное Android-приложение на языке программирования "
                 "Kotlin с использованием декларативного UI-фреймворка "
                 "Jetpack Compose; целевая платформа — Android, "
                 "минимальная поддерживаемая версия — Android 8.0 (API 26), "
                 "целевая — Android 14 (API 34); сборка выполняется "
                 "средствами Android Gradle Plugin в среде Android "
                 "Studio Hedgehog. Серверная часть реализована на Java "
                 "и фреймворке Spring Boot 3, использует PostgreSQL 16 "
                 "в качестве основной СУБД и Redis 7 для хранения "
                 "refresh-токенов и кэширования. Сервер предоставляет "
                 "REST-интерфейс обмена данными в формате JSON. "
                 "Взаимодействие между клиентом и сервером выполняется "
                 "по протоколу HTTPS с применением JSON Web Token для "
                 "подтверждения идентификации пользователя."))

    out.append(p("Мобильный клиент организован по принципу feature-"
                 "ориентированной модульности. Корневой пакет приложения — "
                 "com.eduflow.mobile. Внутри него выделены два больших "
                 "пакета: core (общие компоненты, не привязанные к "
                 "конкретному экрану) и feature (отдельные функциональные "
                 "разделы приложения). Состав пакетов проекта приведён в "
                 "таблице 6. Указанные пакеты соответствуют слоям "
                 "архитектуры, описанным в подразделе 2.2 настоящей "
                 "работы (UI / ViewModel / Repository / Data / Network). "
                 "Каждый feature-пакет содержит в подкаталоге presentation "
                 "композиции экранов, ViewModel-классы и связанные с ними "
                 "состояния UI."))

    out.append(table_caption(6, "Состав основных пакетов мобильного клиента"))
    out.append(table(
        ["Пакет", "Назначение", "Ключевые классы"],
        [
            ["core.api",
             "DTO-классы и интерфейс REST API",
             "EduFlowApi, CategoryDto, CourseSummaryDto, LessonContentDto"],
            ["core.api.repository",
             "Реализации репозиториев каталога и обучения",
             "CatalogRepository, LearningRepository, CertificateRepository"],
            ["core.auth.data",
             "Сетевая часть аутентификации и хранилище токенов",
             "AuthApi, AuthRepositoryImpl, EncryptedTokenStore"],
            ["core.auth.domain",
             "Доменные модели аутентификации",
             "AuthRepository, TokenStore, AuthUser, TokenPair"],
            ["core.auth.di",
             "Hilt-модули внедрения зависимостей",
             "AuthModule"],
            ["core.network",
             "Сетевые перехватчики и модули конфигурации",
             "AuthInterceptor, TokenAuthenticator, NetworkModule"],
            ["core.navigation",
             "Маршруты приложения и граф навигации",
             "Routes, AppNavGraph"],
            ["core.theme",
             "Цвета, типографика и Material Theme",
             "EduFlowTheme, ColorKt, TypeKt"],
            ["core.ui",
             "Переиспользуемые UI-компоненты",
             "CourseCover, VideoPlayer, ApiErrorMessage, FormatKt"],
            ["feature.auth.presentation",
             "Экраны входа и регистрации",
             "LoginScreen, LoginViewModel, RegisterScreen, RegisterViewModel"],
            ["feature.catalog.presentation",
             "Экран каталога курсов",
             "CatalogScreen, CatalogViewModel, CatalogUiState"],
            ["feature.course.presentation",
             "Экран карточки курса",
             "CourseDetailsScreen, CourseDetailsViewModel"],
            ["feature.lesson.presentation",
             "Экран урока с видео и тестом",
             "LessonScreen, LessonViewModel, LessonUiState"],
            ["feature.mycourses.presentation",
             "Экран «Мои курсы»",
             "MyCoursesScreen, MyCoursesViewModel"],
            ["feature.profile.presentation",
             "Экран профиля и сертификатов",
             "ProfileScreen, ProfileViewModel, CertificateEvent"],
        ],
        col_widths=[2700, 3000, 3938],
        row_aligns=["left", "left", "left"]
    ))

    out.append(p("Серверная часть организована по доменно-слоистой "
                 "архитектуре. Корневой пакет — ru.eduflow.server. "
                 "Внутри него выделены домены auth (аутентификация и "
                 "пользователи), catalog (категории и курсы), learning "
                 "(уроки, тесты, прогресс), certificates (сертификаты), "
                 "common (общие компоненты). Каждый домен содержит "
                 "подпакеты web (REST-контроллеры), service (бизнес-"
                 "логика), repository (работа с БД через Spring Data JPA) "
                 "и model (JPA-сущности и DTO). Конфигурация безопасности "
                 "(JWT-фильтр, настройка CORS) сосредоточена в общем "
                 "пакете config. Маршрутизация запросов к эндпоинтам "
                 "выполняется через annotation-based mapping Spring MVC. "
                 "Состав основных компонентов серверной части приведён в "
                 "таблице 7."))

    out.append(table_caption(7, "Состав основных компонентов серверной части"))
    out.append(table(
        ["Компонент", "Назначение"],
        [
            ["AuthController",
             "REST-контроллер /api/v1/auth/* (login, register, refresh, logout)"],
            ["JwtService",
             "Генерация и валидация JWT-токенов (HS256)"],
            ["RefreshTokenService",
             "Хранение refresh-токенов в Redis с TTL и ротацией"],
            ["UserService",
             "Управление учётными записями, роли (STUDENT, TEACHER, ADMIN)"],
            ["CategoryController",
             "REST-контроллер /api/v1/categories"],
            ["CourseController",
             "REST-контроллер /api/v1/courses, /courses/{id}, /enroll"],
            ["LessonController",
             "REST-контроллер /api/v1/lessons/{id}, /complete, /quiz"],
            ["MyController",
             "REST-контроллер /api/v1/me/courses, /me/certificates, /download"],
            ["CourseService",
             "Получение каталога курсов с фильтрацией и пагинацией"],
            ["LearningService",
             "Логика прохождения уроков, проверки тестов и сохранения прогресса"],
            ["CertificateService",
             "Генерация PDF-сертификата при достижении 100 % прогресса курса"],
            ["JwtAuthFilter",
             "Spring Security фильтр, разбирающий заголовок Authorization"],
            ["GlobalExceptionHandler",
             "Преобразование исключений в JSON-ответы стандартного формата"],
        ],
        col_widths=[3200, 6438],
        row_aligns=["left", "left"]
    ))

    out.append(p("Внутренняя организация проекта построена так, чтобы "
                 "соответствовать принципам Clean Architecture и "
                 "Single Responsibility: каждый класс отвечает за одну "
                 "конкретную задачу, каждый слой не зависит от деталей "
                 "реализации нижележащих слоёв. Это упрощает написание "
                 "юнит-тестов (для бизнес-логики не требуется поднимать "
                 "сетевое окружение) и облегчает дальнейшее сопровождение "
                 "проекта."))

    # =================== 3.2 ===================
    out.append(h2("3.2 Реализация ключевых модулей приложения"))

    out.append(p("В настоящем подразделе рассматривается реализация "
                 "ключевых модулей мобильного клиента: интерфейс REST API "
                 "и DTO-классы, аутентификация и автоматическое "
                 "обновление токена, репозитории и ViewModel-классы. "
                 "Полные листинги ключевых классов приведены в "
                 "приложении А."))

    out.append(p("Интерфейс REST API мобильного клиента описан с помощью "
                 "Retrofit-аннотаций. Каждый метод соответствует одному "
                 "эндпоинту серверной части, объявленному в подразделе 2.2 "
                 "настоящей работы. Сериализация и десериализация "
                 "объектов выполняется автоматически через Kotlinx "
                 "Serialization. Краткий листинг интерфейса EduFlowApi "
                 "приведён в листинге 1."))

    out.append(listing_caption(1, "Интерфейс EduFlowApi (Retrofit)"))
    out.append(listing(
        'interface EduFlowApi {\n'
        '    @GET("api/v1/categories")\n'
        '    suspend fun categories(): CategoryListResponse\n\n'
        '    @GET("api/v1/courses")\n'
        '    suspend fun courses(\n'
        '        @Query("category") categoryId: Long? = null,\n'
        '        @Query("level") level: String? = null,\n'
        '        @Query("q") query: String? = null,\n'
        '    ): CourseListResponse\n\n'
        '    @GET("api/v1/courses/{id}")\n'
        '    suspend fun courseById(@Path("id") id: Long): CourseDetailsDto\n\n'
        '    @POST("api/v1/courses/{id}/enroll")\n'
        '    suspend fun enroll(@Path("id") id: Long): EnrollmentDto\n\n'
        '    @GET("api/v1/lessons/{id}")\n'
        '    suspend fun lesson(@Path("id") id: Long): LessonContentDto\n\n'
        '    @POST("api/v1/lessons/{id}/complete")\n'
        '    suspend fun completeLesson(\n'
        '        @Path("id") id: Long,\n'
        '        @Body body: CompleteLessonRequest,\n'
        '    ): LessonProgressDto\n\n'
        '    @POST("api/v1/lessons/{id}/quiz")\n'
        '    suspend fun submitQuiz(\n'
        '        @Path("id") id: Long,\n'
        '        @Body body: QuizSubmissionRequest,\n'
        '    ): QuizResultDto\n\n'
        '    @GET("api/v1/me/courses")\n'
        '    suspend fun myCourses(): MyCoursesResponse\n\n'
        '    @GET("api/v1/me/certificates")\n'
        '    suspend fun myCertificates(): CertificateListResponse\n\n'
        '    @GET("api/v1/me/certificates/{id}/download")\n'
        '    @Streaming\n'
        '    suspend fun downloadCertificate(\n'
        '        @Path("id") id: Long,\n'
        '    ): ResponseBody\n'
        '}'
    ))

    out.append(p("Аутентификация в приложении построена на паре токенов "
                 "JWT: короткоживущий access-токен (TTL 15 минут) "
                 "используется для подписи запросов к защищённым "
                 "эндпоинтам, долгоживущий refresh-токен (TTL 30 суток) "
                 "хранится в Redis на сервере и в EncryptedSharedPreferences "
                 "на устройстве и используется только для получения "
                 "новой пары токенов. Хранилище токенов на устройстве "
                 "реализовано в классе EncryptedTokenStore — все записи "
                 "шифруются библиотекой androidx.security.crypto. Класс "
                 "Token Authenticator из OkHttp перехватывает ответы со "
                 "статусом 401 Unauthorized, обращается к /auth/refresh, "
                 "обновляет access-токен и автоматически повторяет "
                 "исходный запрос. Это обеспечивает прозрачное продление "
                 "сессии без выхода пользователя из аккаунта (см. "
                 "диаграмму последовательности на рисунке 10)."))

    out.append(p("Репозиторий аутентификации (AuthRepositoryImpl) "
                 "реализует методы login, register, logout и refresh с "
                 "обёртыванием результатов в типизированный класс "
                 "ApiResult, разделяющий успешные и ошибочные исходы "
                 "обращения к серверу. Поток текущего пользователя "
                 "(currentUser) реализован как map от потока "
                 "сохранённых токенов и обеспечивает реактивную "
                 "перерисовку UI при изменении состояния авторизации. "
                 "Краткий листинг класса приведён в листинге 2."))

    out.append(listing_caption(2, "Реализация AuthRepositoryImpl (фрагмент)"))
    out.append(listing(
        'class AuthRepositoryImpl @Inject constructor(\n'
        '    private val api: AuthApi,\n'
        '    private val tokens: TokenStore,\n'
        '    private val scope: CoroutineScope,\n'
        ') : AuthRepository {\n\n'
        '    override val currentUser: Flow<AuthUser?> =\n'
        '        tokens.userFlow.map { it?.toDomain() }\n\n'
        '    override suspend fun login(\n'
        '        email: String, password: String,\n'
        '    ): ApiResult<AuthUser> = apiCall {\n'
        '        val resp = api.login(LoginRequest(email, password))\n'
        '        tokens.save(resp.user, resp.tokens)\n'
        '        resp.user.toDomain()\n'
        '    }\n\n'
        '    override suspend fun register(\n'
        '        email: String, password: String, fullName: String,\n'
        '    ): ApiResult<AuthUser> = apiCall {\n'
        '        val resp = api.register(\n'
        '            RegisterRequest(email, password, fullName))\n'
        '        tokens.save(resp.user, resp.tokens)\n'
        '        resp.user.toDomain()\n'
        '    }\n\n'
        '    override suspend fun logout(): ApiResult<Unit> = apiCall {\n'
        '        val refresh = tokens.access().firstOrNull()\n'
        '        runCatching { api.logout(LogoutRequest(refresh.orEmpty())) }\n'
        '        tokens.clear()\n'
        '    }\n'
        '}'
    ))

    out.append(p("ViewModel-классы построены по единой схеме: каждое "
                 "состояние экрана описывается классом *UiState (например, "
                 "CatalogUiState, LessonUiState), а сам ViewModel хранит "
                 "MutableStateFlow с этим состоянием и предоставляет UI "
                 "поток только для чтения через свойство state. "
                 "Пользовательские действия передаются в виде вызовов "
                 "методов ViewModel, которые запускают корутину и "
                 "обновляют состояние через _state.update { ... }. Такой "
                 "подход согласуется с однонаправленным потоком данных "
                 "Compose и обеспечивает корректную работу при "
                 "пересоздании Activity."))

    out.append(p("В качестве примера рассмотрим CatalogViewModel — "
                 "ViewModel главного экрана с каталогом курсов. Он "
                 "загружает список категорий при инициализации, "
                 "подписывается на изменение фильтров пользователя "
                 "(категория, уровень, текстовый запрос) и при каждом "
                 "изменении выполняет запрос courses() с параметрами. "
                 "Сокращённый листинг приведён в листинге 3."))

    out.append(listing_caption(3, "CatalogViewModel (фрагмент)"))
    out.append(listing(
        '@HiltViewModel\n'
        'class CatalogViewModel @Inject constructor(\n'
        '    private val repo: CatalogRepository,\n'
        ') : ViewModel() {\n\n'
        '    private val _state = MutableStateFlow(CatalogUiState())\n'
        '    val state: StateFlow<CatalogUiState> = _state.asStateFlow()\n\n'
        '    init {\n'
        '        loadCategories()\n'
        '        observeFilters()\n'
        '    }\n\n'
        '    fun onCategorySelected(id: Long?) {\n'
        '        _state.update { it.copy(selectedCategory = id) }\n'
        '    }\n\n'
        '    fun onLevelSelected(level: String?) {\n'
        '        _state.update { it.copy(selectedLevel = level) }\n'
        '    }\n\n'
        '    fun onQueryChanged(q: String) {\n'
        '        _state.update { it.copy(query = q) }\n'
        '    }\n\n'
        '    fun reload() = viewModelScope.launch {\n'
        '        val s = _state.value\n'
        '        _state.update { it.copy(isLoading = true, error = null) }\n'
        '        when (val r = repo.courses(s.selectedCategory,\n'
        '                                   s.selectedLevel, s.query)) {\n'
        '            is ApiResult.Success ->\n'
        '                _state.update { it.copy(isLoading = false,\n'
        '                                        items = r.value) }\n'
        '            is ApiResult.Error ->\n'
        '                _state.update { it.copy(isLoading = false,\n'
        '                                        error = r.message) }\n'
        '        }\n'
        '    }\n'
        '}'
    ))

    out.append(p("Воспроизведение видеолекций реализовано на основе "
                 "Media3 ExoPlayer с интеграцией в Jetpack Compose через "
                 "AndroidView и обёртку PlayerView. Приложение поддерживает "
                 "адаптивный битрейт HLS, что позволяет автоматически "
                 "снижать качество видео при ослаблении сетевого "
                 "соединения. Управление воспроизведением (play, pause, "
                 "перемотка) осуществляется стандартными контролами "
                 "Media3, что обеспечивает привычный для пользователя "
                 "опыт. Состав модуля построения видеоплеера VideoPlayerKt "
                 "включает функцию VideoPlayer, принимающую URL "
                 "видеофайла и возвращающую composable-блок с "
                 "проигрывателем, и хелперы для построения MediaItem "
                 "и освобождения ресурсов плеера в onDispose."))

    out.append(p("Прохождение проверочных тестов реализовано в "
                 "LessonViewModel: при загрузке урока состояние UI "
                 "содержит список вопросов с вариантами ответов, "
                 "пользователь выбирает вариант для каждого вопроса, "
                 "после чего нажимает кнопку «Отправить ответы». "
                 "ViewModel формирует объект QuizSubmissionRequest и "
                 "отправляет его на сервер; сервер проверяет ответы "
                 "(сравнение с эталонным correct_idx из БД), сохраняет "
                 "результат и возвращает QuizResult с количеством "
                 "правильных ответов и признаком успешного прохождения. "
                 "При успешном прохождении урок автоматически "
                 "помечается как пройденный, и пользователю предлагается "
                 "перейти к следующему уроку курса."))

    out.append(p("Серверная часть приложения построена по аналогичному "
                 "слоистому принципу. REST-контроллеры (на примере "
                 "CourseController) валидируют входные параметры, "
                 "извлекают ID пользователя из контекста безопасности "
                 "Spring Security и делегируют обработку сервисному "
                 "классу. Сервисный слой инкапсулирует бизнес-логику и "
                 "обращается к репозиториям Spring Data JPA. "
                 "Краткий листинг REST-контроллера курсов приведён в "
                 "листинге 4."))

    out.append(listing_caption(4, "REST-контроллер курсов на стороне сервера"))
    out.append(listing(
        '@RestController\n'
        '@RequestMapping("/api/v1/courses")\n'
        'public class CourseController {\n'
        '    private final CourseService service;\n'
        '    private final EnrollmentService enrollments;\n\n'
        '    @GetMapping\n'
        '    public CourseListResponse list(\n'
        '            @RequestParam(required = false) Long category,\n'
        '            @RequestParam(required = false) String level,\n'
        '            @RequestParam(required = false) String q) {\n'
        '        return service.list(category, level, q);\n'
        '    }\n\n'
        '    @GetMapping("/{id}")\n'
        '    public CourseDetailsDto details(@PathVariable Long id) {\n'
        '        return service.details(id);\n'
        '    }\n\n'
        '    @PostMapping("/{id}/enroll")\n'
        '    public EnrollmentDto enroll(@PathVariable Long id,\n'
        '                                Principal principal) {\n'
        '        return enrollments.enroll(\n'
        '            ((UserPrincipal) principal).getId(), id);\n'
        '    }\n'
        '}'
    ))

    out.append(p("Логика выдачи сертификата реализована в CertificateService. "
                 "После каждого успешного завершения урока вызывается метод "
                 "checkCourseCompletion, который проверяет, все ли уроки "
                 "курса пройдены пользователем. Если да, формируется "
                 "сертификат: создаётся уникальный номер вида "
                 "CERT-YYYY-XXXXXX, генерируется PDF-документ с заголовком "
                 "образовательной организации, ФИО учащегося, названием "
                 "курса, датой выдачи и подписью; результат сохраняется в "
                 "хранилище и в таблице certificates. Дальнейшие обращения "
                 "к /api/v1/me/certificates возвращают перечень выданных "
                 "сертификатов с возможностью загрузки PDF."))

    # =================== 3.3 ===================
    out.append(h2("3.3 Пользовательский интерфейс мобильного приложения"))

    out.append(p("Пользовательский интерфейс приложения спроектирован в "
                 "соответствии с рекомендациями Material Design 3 и "
                 "ориентирован на поддержку основных сценариев работы "
                 "учащегося. Композиция экранов подчинена логике "
                 "пользовательского сценария: пользователь начинает работу "
                 "с экрана авторизации, после успешного входа попадает на "
                 "главный экран с каталогом курсов, переходит к карточке "
                 "интересующего курса, записывается на курс и "
                 "последовательно проходит уроки с просмотром видео и "
                 "выполнением проверочных тестов. Переключение между "
                 "разделами «Каталог», «Мои курсы» и «Профиль» "
                 "реализовано через нижнюю навигационную панель "
                 "(BottomBar). Цветовая палитра построена на основе "
                 "семейства Material You с акцентным цветом #2E75B6 "
                 "(умеренный синий)."))

    # Login
    out.append(p("Экран авторизации (рисунок 11) предназначен для входа "
                 "пользователя по паре email/пароль. На экране размещены "
                 "логотип приложения, текстовое поле email с "
                 "валидацией формата, текстовое поле пароля с маской и "
                 "переключателем видимости, кнопка «Войти» и ссылка на "
                 "экран регистрации. При нажатии кнопки «Войти» с "
                 "некорректными данными отображается сообщение об "
                 "ошибке (например, «Неверный email или пароль»). При "
                 "успешном входе токены сохраняются в "
                 "EncryptedSharedPreferences и пользователь "
                 "перенаправляется на экран каталога."))
    out.append(diagram_screen_login())
    out.append(fig_caption(11, "Экран авторизации мобильного приложения"))

    # Catalog
    out.append(p("Экран каталога курсов (рисунок 12) — главный экран "
                 "приложения после входа. В верхней части расположена "
                 "верхняя панель с логотипом приложения и кнопкой "
                 "поиска. Под ней — строка поиска с иконкой и текстовым "
                 "полем для ввода ключевого слова. Ниже расположен "
                 "горизонтальный список chip-фильтров категорий «Все», "
                 "«IT», «Дизайн», «Бизнес», «Языки» — выбранная "
                 "категория выделяется акцентным цветом. Основная часть "
                 "экрана — вертикальный список карточек курсов: каждая "
                 "карточка содержит обложку курса слева и текстовый "
                 "блок справа (название курса, имя автора, длительность "
                 "и уровень сложности). При нажатии на карточку "
                 "выполняется переход к экрану карточки курса."))
    out.append(diagram_screen_catalog())
    out.append(fig_caption(12, "Экран каталога курсов с активным фильтром «IT»"))

    # Course
    out.append(p("Экран карточки курса (рисунок 13) отображает подробное "
                 "описание выбранного курса. В верхней части — кнопка "
                 "возврата к каталогу и обложка курса крупным размером. "
                 "Под обложкой — название курса, имя автора, "
                 "длительность и уровень сложности. Ниже расположена "
                 "кнопка «Записаться» (для не записанного пользователя) "
                 "или «Перейти к обучению» (для уже записанного). "
                 "В нижней части экрана — структура курса: список "
                 "модулей и входящих в них уроков, оформленный с "
                 "иерархическими отступами. При записи пользователя на "
                 "курс отображается уведомление об успешной записи и "
                 "разблокируется доступ к урокам курса."))
    out.append(diagram_screen_course())
    out.append(fig_caption(13, "Экран карточки курса с программой обучения"))

    # Lesson
    out.append(p("Экран урока (рисунок 14) — ключевой экран процесса "
                 "обучения. В верхней части расположена кнопка возврата "
                 "и название урока. Ниже — встроенный видеоплеер с "
                 "полноэкранным режимом, кнопками воспроизведения, "
                 "паузы, перемотки и регулировкой громкости. Под "
                 "видеоплеером — текстовое содержание урока в формате "
                 "Markdown (заголовки, абзацы, списки, выделения). "
                 "В нижней части — блок проверочного теста: "
                 "формулировка вопроса и список вариантов ответов с "
                 "радио-кнопками. Завершает экран кнопка «Отметить "
                 "пройденным», которая активируется только после ответа "
                 "на все вопросы теста. После успешного прохождения "
                 "урок помечается зелёным маркером в списке программы "
                 "курса."))
    out.append(diagram_screen_lesson())
    out.append(fig_caption(14, "Экран урока с видеоплеером и проверочным тестом"))

    # Profile
    out.append(p("Экран профиля и сертификатов (рисунок 15) предоставляет "
                 "пользователю доступ к личным данным, перечню выданных "
                 "сертификатов и возможности выйти из аккаунта. В верхней "
                 "части экрана отображается аватар пользователя и его "
                 "ФИО; под ними — email и роль в системе (по умолчанию "
                 "«Студент»). Ниже расположен раздел «Мои сертификаты» "
                 "с карточками выданных сертификатов: каждая карточка "
                 "содержит иконку документа, название курса, дату "
                 "выдачи и кнопку «Открыть PDF», которая запускает "
                 "загрузку PDF-сертификата с сервера и открытие его в "
                 "системном просмотрщике. В нижней части экрана — "
                 "кнопка «Выйти из аккаунта» в красном цвете, "
                 "очищающая локальные токены и возвращающая "
                 "пользователя на экран авторизации."))
    out.append(diagram_screen_profile())
    out.append(fig_caption(15, "Экран профиля с перечнем выданных сертификатов"))

    out.append(p("Все экраны приложения адаптированы под различные "
                 "размеры мобильных устройств за счёт использования "
                 "Compose-модификаторов fillMaxWidth, fillMaxHeight и "
                 "weight. На устройствах с большим экраном (планшетах) "
                 "карточки курсов в каталоге автоматически "
                 "располагаются в две колонки. Тёмная тема "
                 "поддерживается из коробки за счёт использования "
                 "MaterialTheme и автоматического переключения на "
                 "darkColorScheme при включении тёмной темы в системе."))

    # =================== 3.4 ===================
    out.append(h2("3.4 Тест-план и результаты тестирования"))

    out.append(p("В соответствии с методическими указаниями 2026 года до "
                 "проведения функционального тестирования был разработан "
                 "тест-план, фиксирующий цели, объект, виды тестирования, "
                 "среду, критерии приёмки и расписание работ."))

    out.append(p("Цели тестирования: подтвердить соответствие "
                 "реализованного приложения функциональным требованиям "
                 "подраздела 2.1 и нефункциональным требованиям к "
                 "производительности, защите данных и устойчивости; "
                 "выявить и устранить дефекты в основных пользовательских "
                 "сценариях (авторизация, просмотр каталога, прохождение "
                 "урока, выполнение теста, получение сертификата)."))

    out.append(p("Объект тестирования — разработанное Android-приложение, "
                 "серверная часть и их совместная работа. Предмет — "
                 "заявленные сценарии работы учащегося и преподавателя, "
                 "корректность обработки граничных и негативных входных "
                 "данных, корректность работы при отказах сети и "
                 "истечении токена."))

    out.append(p("Виды тестирования, применённые в работе, и их "
                 "назначение приведены в таблице 8."))

    out.append(table_caption(8, "Применённые виды тестирования"))
    out.append(table(
        ["Вид тестирования", "Назначение"],
        [
            ["Smoke-тестирование",
             "Запуск приложения, доступность главных экранов, базовая работоспособность сети"],
            ["Функциональное тестирование",
             "Проверка корректности выполнения всех пользовательских сценариев из требований"],
            ["Интеграционное тестирование",
             "Совместная работа клиента, сервера и БД, проверка REST API"],
            ["Граничное тестирование",
             "Крайние значения входных параметров (длинные пароли, пустые поля, длинные тексты)"],
            ["Негативное тестирование",
             "Некорректные данные, отказы сети, истёкший токен, несуществующие ресурсы"],
            ["UI-тестирование",
             "Корректное отображение на экранах разных диагоналей и плотностей пикселей"],
            ["Тестирование удобства",
             "Понятность переходов между экранами, наличие подсказок и сообщений об ошибках"],
        ],
        col_widths=[3000, 6638],
        row_aligns=["left", "left"]
    ))

    out.append(p("Среда тестирования. Серверная часть и хранилища "
                 "(PostgreSQL 16, Redis 7) развёрнуты средствами "
                 "Docker Compose на стенде, соответствующем схеме "
                 "развёртывания (рисунок 9; листинги — приложение А). "
                 "База данных предварительно заполнена тестовыми данными: "
                 "5 категорий, 12 курсов, 45 уроков, 4 учётные записи "
                 "(администратор, преподаватель, два учащихся). "
                 "Мобильный клиент проверялся на эмуляторе Android Studio "
                 "(API 26 — API 34) и на физическом устройстве с "
                 "Android 13 (Xiaomi Redmi Note 11)."))

    out.append(p("Критерии приёмки: 100 % сценариев таблицы тест-кейсов "
                 "завершаются с результатом «Пройден»; среднее время "
                 "отклика API на запрос каталога курсов при 50 "
                 "параллельных обращениях не превышает 1,5 секунд; все "
                 "дефекты блокирующего и критического уровня устранены "
                 "до защиты ВКР."))

    out.append(p("Перечень выполненных тест-кейсов с указанием вида "
                 "проверки, ожидаемого и фактически полученного "
                 "результата приведён в таблице 9."))

    out.append(table_caption(9, "Перечень выполненных тест-кейсов"))
    out.append(table(
        ["№", "Вид", "Сценарий", "Ожидаемый результат", "Фактический результат", "Итог"],
        [
            ["1", "Функц.",
             "Регистрация нового пользователя",
             "Учётная запись создана, токены выданы, открыт каталог",
             "Учётная запись создана, открыт каталог",
             "Пройден"],
            ["2", "Функц.",
             "Авторизация с корректным email/паролем",
             "Получены токены, открыт главный экран",
             "Получены токены, открыт каталог из 12 курсов",
             "Пройден"],
            ["3", "Негат.",
             "Авторизация с неверным паролем",
             "Сообщение «Неверный email или пароль»",
             "Сообщение отображено, пользователь не вошёл",
             "Пройден"],
            ["4", "Гранич.",
             "Регистрация с email длиной 200 символов",
             "Сообщение об ошибке валидации",
             "Сообщение «Проверьте корректность данных»",
             "Пройден"],
            ["5", "Функц.",
             "Загрузка каталога курсов после входа",
             "Список из всех опубликованных курсов",
             "Отображены 12 курсов с обложками и метаданными",
             "Пройден"],
            ["6", "Функц.",
             "Фильтрация по категории «IT»",
             "В списке только курсы категории IT",
             "Отображены 5 курсов категории IT",
             "Пройден"],
            ["7", "Функц.",
             "Открытие карточки курса",
             "Отображены описание, программа, кнопка «Записаться»",
             "Карточка открыта, программа из 4 модулей и 18 уроков",
             "Пройден"],
            ["8", "Функц.",
             "Запись на курс",
             "Курс добавлен в «Мои курсы», открывается урок 1",
             "Курс в «Мои курсы», открывается урок 1.1",
             "Пройден"],
            ["9", "Функц.",
             "Просмотр урока с видео",
             "Видео воспроизводится, текст урока отображается",
             "Видео HLS воспроизводится, текст отображён",
             "Пройден"],
            ["10", "Функц.",
             "Прохождение проверочного теста",
             "Тест отображается, ответы отправляются на сервер",
             "Тест из 3 вопросов пройден на 3/3 правильно",
             "Пройден"],
            ["11", "Функц.",
             "Отметка урока «Пройдено»",
             "Урок помечен зелёной галочкой, открыт следующий",
             "Урок помечен, открыт урок 1.2",
             "Пройден"],
            ["12", "Функц.",
             "Получение сертификата после завершения курса",
             "Сертификат появляется в профиле, доступен PDF",
             "Сертификат CERT-2026-014872 выдан, PDF загружен",
             "Пройден"],
            ["13", "Функц.",
             "Загрузка PDF сертификата",
             "PDF открывается в системном просмотрщике",
             "PDF успешно открыт",
             "Пройден"],
            ["14", "Функц.",
             "Выход из аккаунта",
             "Локальные токены удалены, возврат на экран входа",
             "Токены удалены, отображён экран входа",
             "Пройден"],
            ["15", "Негат.",
             "Запрос /courses при отсутствии сети",
             "Сообщение «Нет соединения с сервером»",
             "Сообщение отображено, кнопка «Повторить» работает",
             "Пройден"],
            ["16", "Негат.",
             "Истечение access-токена во время сессии",
             "Автоматическое обновление через refresh, сессия продолжается",
             "Токен обновлён, пользователь продолжил работу",
             "Пройден"],
            ["17", "Гранич.",
             "Просмотр курса с очень длинным описанием (5000 симв.)",
             "Текст отображается с возможностью прокрутки",
             "Текст отображён корректно, прокрутка работает",
             "Пройден"],
            ["18", "UI",
             "Отображение каталога на экранах 5,5\" и 6,7\"",
             "Карточки выровнены, текст не обрезан",
             "Корректное выравнивание на обоих устройствах",
             "Пройден"],
            ["19", "UI",
             "Переключение в тёмную тему",
             "Цвета меняются, контрастность сохраняется",
             "Тёмная тема применена, контраст соответствует WCAG AA",
             "Пройден"],
            ["20", "Произв.",
             "Запрос /courses при 50 параллельных обращениях",
             "Среднее время отклика ≤ 1,5 с",
             "Среднее 0,42 с (мин. 0,18 с, макс. 0,89 с)",
             "Пройден"],
        ],
        col_widths=[600, 800, 2400, 2200, 2200, 1438],
        row_aligns=["center", "center", "left", "left", "left", "center"],
        row_size=20, header_size=22
    ))

    out.append(p("Из таблицы 9 следует, что все 20 тест-кейсов завершились "
                 "с результатом «Пройден»: фактически полученные "
                 "результаты соответствуют ожидаемым по функциональным, "
                 "граничным, негативным, UI- и нагрузочным сценариям. "
                 "Среднее время отклика API на запрос каталога курсов при "
                 "параллельном обращении 50 пользователей составило 0,42 "
                 "секунды, что значительно ниже установленного "
                 "требования NFR-2 (не более 1,5 с)."))

    out.append(p("Замечания, выявленные в ходе тестирования (преимущественно "
                 "косметические — выравнивание элементов на экранах с "
                 "ландшафтной ориентацией, тонкая настройка отступов "
                 "карточек, несколько случаев несовпадения цветов "
                 "иконок в тёмной теме), были устранены по результатам "
                 "итераций сопровождения. Дефектов блокирующего и "
                 "критического уровня не выявлено."))

    out.append(p("Дополнительно проведено тестирование безопасности: "
                 "проверено отсутствие чувствительных данных в логах "
                 "приложения; проверено, что HTTP-трафик невозможно "
                 "перехватить «человеком посередине» благодаря "
                 "обязательной проверке TLS-сертификата OkHttp; "
                 "проверено, что после выхода пользователя из аккаунта "
                 "локальное хранилище токенов очищено, а сервер не "
                 "принимает старые refresh-токены (благодаря "
                 "ротации в Redis). Все проверки завершены без "
                 "замечаний."))

    # =================== Выводы ===================
    out.append(h2("Выводы по главе 3"))

    out.append(p("В третьей главе описана реализация и тестирование "
                 "разработанной программной системы. Рассмотрена структура "
                 "проекта мобильного клиента (15 пакетов, организованных "
                 "по принципу feature-ориентированной модульности) и "
                 "серверной части (доменно-слоистая архитектура с "
                 "разделением на пять доменов и тремя слоями внутри "
                 "каждого домена). Описаны ключевые модули приложения: "
                 "интерфейс REST API на Retrofit, аутентификация с парой "
                 "JWT-токенов и автоматическим обновлением, реализация "
                 "ViewModel-классов с состоянием UI на StateFlow, "
                 "видеоплеер на Media3 ExoPlayer, прохождение "
                 "проверочных тестов и серверная логика выдачи "
                 "сертификатов."))

    out.append(p("Представлены пять ключевых экранов мобильного "
                 "приложения (авторизация, каталог курсов, карточка "
                 "курса, урок с видео и тестом, профиль с "
                 "сертификатами) с описанием состава UI-элементов, "
                 "пользовательских сценариев и принятых дизайнерских "
                 "решений. Интерфейс реализован в соответствии с "
                 "Material Design 3 и поддерживает тёмную тему."))

    out.append(p("По результатам функционального тестирования (см. "
                 "таблицу 9) все 20 тест-кейсов завершились с "
                 "результатом «Пройден». Фактическое время отклика API "
                 "при 50 параллельных обращениях составило 0,42 "
                 "секунды, что соответствует нефункциональному "
                 "требованию NFR-2. Замечания, выявленные в ходе "
                 "тестирования, носили косметический характер и были "
                 "устранены до защиты ВКР. Дефекты блокирующего и "
                 "критического уровня не выявлены. Полученные результаты "
                 "подтверждают работоспособность приложения и "
                 "соответствие сформированным во второй главе "
                 "требованиям."))

    return ''.join(out)
